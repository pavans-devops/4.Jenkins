pipeline {
    agent {
        label 'master'
    }
    stages {
        stage ('PRINT') {
            steps {
                sh 'echo it is printing'
            }
        }
        stage ('WRITE') {
            steps {
                sh 'echo it is writing'
            }
        }
    }
}
